# ระบบประเมินบุคลากร (Employee Evaluation System)

เว็บแอปพลิเคชันรูปแบบทันสมัย (Modern Full-Stack) ที่ออกแบบมาเพื่อบริหารจัดการการประเมินผลการทำงานของบุคลากรภายในองค์กร ระบบถูกออกแบบโดยใช้สถาปัตยกรรมแบบแบ่งแยกตามบทบาท (Role-Based Architecture) รองรับผู้ดูแลระบบ (Administrator), ผู้ประเมิน (Evaluator), และผู้ถูกประเมิน (Evaluatee) พร้อมด้วยส่วนติดต่อผู้ใช้งาน (UI) ที่สวยงามและตอบสนองได้ดีเยี่ยม (Responsive)

## 🚀 ฟีเจอร์หลัก (Features)

### 👤 การจัดการสิทธิ์การเข้าถึง (Role-Based Access Control)
- **ผู้ดูแลระบบ (ADMIN)**: เข้าถึงภาพรวมของระบบได้ทั้งหมด, จัดการผู้ใช้งาน, และบริหารจัดการวงจรโครงการประเมินผล
- **ผู้ประเมิน (EVALUATOR)**: สามารถดูรายชื่อผู้ที่ตนเองต้องประเมิน, กรองงานตามสถานะ, และทำการส่งผลประเมินได้
- **ผู้ถูกประเมิน (EVALUATEE)**: สามารถดูผลการประเมินของตนเอง, ดาวน์โหลดรายงาน PDF, และส่งไฟล์หลักฐานประกอบการประเมิน

### 📊 แดชบอร์ดผู้ดูแลระบบ (Admin Dashboard)
- **สถิติภาพรวม**: การ์ดแสดงผลแบบเรียลไทม์ (Real-time) แจ้งยอดรวมผู้ใช้งาน, จำนวนแบบประเมินทั้งหมด, และสถานะการมอบหมายงาน
- **รายงานขั้นสูง (Advanced Reporting)**: 
  - **กราฟความคืบหน้า (Progress Chart)**: ติดตามเปอร์เซ็นต์การส่งงานแยกรายแผนก
  - **วิเคราะห์รายหัวข้อ (Topic Analysis)**: กราฟแท่งแสดงคะแนนเฉลี่ยของแต่ละหัวข้อการประเมิน
  - **Export CSV**: ดาวน์โหลดข้อมูลผลการประเมินดิบเพื่อนำไปวิเคราะห์ต่อได้ทันที
- **การจัดการแบบประเมิน**: สร้าง/แก้ไข/ยกเลิก (Cancel) แบบประเมิน, กำหนดหัวข้อและตัวชี้วัด พร้อมกำหนดน้ำหนักและเงื่อนไขการแนบหลักฐาน
- **จัดการผู้ใช้งานและการมอบหมาย**: บริหารจัดการข้อมูลผู้ใช้งาน (รวมถึงแผนก), สร้างและแก้ไขการจับคู่ผู้ประเมิน (Assignment)

### 📝 ระบบการประเมิน (Evaluation Process)
- **สำหรับผู้ประเมิน**: 
  - ระบบให้คะแนนที่ใช้งานง่าย รองรับทั้งแบบสเกล (1-4) และแบบผ่าน/ไม่ผ่าน (Yes/No)
  - **หมายเหตุ (Remarks)**: สามารถเพิ่มความคิดเห็นประกอบการให้คะแนนในแต่ละข้อได้
  - **ประวัติการแก้ไข (Audit Logs)**: ตรวจสอบย้อนหลังได้ว่ามีการแก้ไขคะแนนเมื่อไหร่และแก้ไขจากค่าใดเป็นค่าใด
  - **ระบบสถานะ**: บันทึกแบบร่าง (Draft) และยืนยันการส่ง (Submitted) เพื่อล็อคผลการประเมิน
- **สำหรับผู้ถูกประเมิน**:
  - **อัปโหลดหลักฐาน (Evidence Upload)**: แนบไฟล์เอกสาร (PDF, DOCX, Images) ประกอบตัวชี้วัดที่กำหนดได้ (รองรับไฟล์ขนาดใหญ่สูงสุด 10MB)
  - **รายงาน PDF**: ดาวน์โหลดสรุปผลการประเมินของตนเองในรูปแบบเอกสาร PDF ที่สวยงาม

### 🎨 ผู้ใช้งานและอินเทอร์เฟซ (Modern UI/UX)
- โครงสร้างหน้าเว็บแบบแถบนำทาง (Sidebar) ด้านข้างที่ตอบสนองต่อขนาดหน้าจอ
- การออกแบบที่สะอาดตาและใช้งานง่าย (Clean & Intuitive) ตามสไตล์เว็บไซต์ยุคใหม่
- ระบบการแจ้งเตือนและการกดยืนยัน (Toast & Confirmations) ด้วย SweetAlert2

## 🛠️ เทคโนโลยีที่ใช้ (Technology Stack)

### ฝั่งหน้าบ้าน (Frontend)
- **Framework**: [Next.js 14+](https://nextjs.org/) (React)
- **การจัดการสไตล์ (Styling)**: [Tailwind CSS](https://tailwindcss.com/)
- **ไอคอน (Icons)**: [Lucide React](https://lucide.dev/)
- **กราฟและแผนภูมิ**: [Chart.js](https://www.chartjs.org/) & [react-chartjs-2](https://react-chartjs-2.js.org/)
- **ระบบแจ้งเตือน**: [SweetAlert2](https://sweetalert2.github.io/)
- **การจัดการเซสชัน**: `js-cookie` สำหรับจัดการ JWT Session

### ฝั่งหลังบ้าน (Backend)
- **API Framework**: Node.js ร่วมกับ [Express](https://expressjs.com/)
- **Database ORM**: [Prisma ORM](https://www.prisma.io/)
- **ฐานข้อมูล**: MySQL (Relational DB)
- **เอกสาร PDF**: `pdfkit` สำหรับสร้างรายงาน PDF
- **จัดการไฟล์**: `multer` สำหรับอัปโหลดไฟล์หลักฐาน

## 📂 โครงสร้างโปรเจกต์ (Project Structure)

- `/frontend` - แอปพลิเคชันฝั่งหน้าบ้าน (Next.js)
  - `/app/dashboard` - ส่วนการแสดงผลหลัก แบ่งแยกโฟลเดอร์ตามสิทธิ์การใช้งาน (`/admin`, `/evaluator`, `/evaluatee`)
  - `/components` - ส่วนประกอบ UI แบบอเนกประสงค์ (Reusable components) เช่น `Sidebar` เป็นต้น
- `/backend` - API เซิร์ฟเวอร์ (Express)
  - `/src/controllers` - Logic การทำงานหลักของระบบ
  - `/src/routes` - กำหนดเส้นทาง API (Routes)
  - `/prisma` - ไฟล์กำหนด Schema ของฐานข้อมูล

## 💻 การติดตั้งและรันโปรเจกต์ (Running Locally)

### สิ่งที่ระบบต้องการ (Prerequisites)
- [Node.js](https://nodejs.org/) (เวอร์ชัน 18 ขึ้นไป)
- `npm` หรือ `yarn`
- ฐานข้อมูล MySQL (ติดตั้ง local หรือใช้ Docker/Cloud)

### 1. การตั้งค่าฝั่งหลังบ้าน (Backend)
1. เข้าไปที่โฟลเดอร์ backend: `cd backend`
2. ติดตั้ง Dependencies: `npm install`
3. ตั้งค่าไฟล์ `.env` โดยระบุ `DATABASE_URL` และ `JWT_SECRET` ให้ถูกต้อง
4. รันคำสั่ง Prisma เพื่อจัดการฐานข้อมูล: `npx prisma migrate dev`
5. เริ่มต้นการทำงานของเซิร์ฟเวอร์: `npm run dev`

### 2. การตั้งค่าฝั่งหน้าบ้าน (Frontend)
1. เข้าไปที่โฟลเดอร์ frontend: `cd frontend`
2. ติดตั้ง Dependencies: `npm install`
3. เริ่มต้นการทำงานของเซิร์ฟเวอร์ (Development): `npm run dev`

### 3. เริ่มใช้งาน (Access the Application)
เปิดเบราว์เซอร์ไปที่ `http://localhost:3000`
